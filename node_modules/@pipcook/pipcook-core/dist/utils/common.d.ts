/// <reference types="node" />
import { PluginSourceProtocol } from '../types/plugins';
import * as url from 'url';
/**
 * parse plugin name, return the plugin protocol and url object
 */
export declare function parsePluginName(name: string): {
    protocol: PluginSourceProtocol;
    urlObject: url.UrlWithStringQuery;
};
